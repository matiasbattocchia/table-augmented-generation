{
  "name": "tag",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8788",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create tag-db",
    "db:migrate": "wrangler d1 execute tag-db --local --file=./src/db/schema.sql",
    "db:migrate:prod": "wrangler d1 execute tag-db --file=./src/db/schema.sql",
    "types": "wrangler types",
    "typecheck": "npm run types && tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.11.4",
    "zod": "^4.3.5"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "wrangler": "^4.59.2"
  }
}
